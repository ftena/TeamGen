<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	android:versionName="1.0" android:versionCode="2" package="com.tarlic.TeamGen">

<uses-sdk android:minSdkVersion="7" 
          android:targetSdkVersion="7"
          android:maxSdkVersion="17" />

	<application android:label="@string/app_name" android:icon="@drawable/ic_launcher">
	
		<!--
			Por cada Activity instanciada durante la ejecución de la aplicación,
			es necesario un <activity> ... </activity>
		-->

		<activity android:name=".TeamGen" android:label="@string/app_name">
		
				<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		
		</activity>
		
		<activity android:name=".Teams" android:label="@string/activity_teams"></activity>

	</application>

        
</manifest> 


